namespace MonGenerator.Type
{
    public enum MonTypeRanges
    {
        attackMin = -100,
        attackMax = 100,
        hpMin = -100,
        hpMax = 100,
        speedMin = -100,
        speedMax = 100,
        statTotalMin = -250,
        statTotalMax = 50
    }
}